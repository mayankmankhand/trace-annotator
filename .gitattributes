# Auto-detect text files and normalize line endings
* text=auto

# Force LF line endings for shell scripts and everything in scripts/
# Prevents CRLF on Windows clones from breaking bash execution
*.sh text eol=lf
scripts/** text eol=lf
